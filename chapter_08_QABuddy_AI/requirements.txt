# Backend + ingestion (frontend has its own requirements.txt)
fastapi>=0.110
uvicorn[standard]>=0.29
qdrant-client>=1.12
groq>=0.11
httpx>=0.27
python-dotenv>=1.0
pandas>=2.1
openpyxl>=3.1            # .xlsx test cases
pypdf>=4.0               # PDF docs / PRDs
fastembed>=0.5           # BM25 sparse side of hybrid (see README: sparse note)

# Optional but recommended ------------------------------------------------
tree-sitter>=0.23              # AST-aware code chunking (regex fallback if absent)
tree-sitter-language-pack>=0.4
python-docx>=1.1               # .docx company docs
mcp>=1.2                       # JIRA sync via the JIRA MCP connection
