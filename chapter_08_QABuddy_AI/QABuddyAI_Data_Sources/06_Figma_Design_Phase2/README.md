# 06 — Figma Design (PHASE 2 — inactive, skip in phase 1)

Ingesters must **skip this folder** in phase 1.

**Phase 2 plan:** Figma REST API → frame names, text layers, component
descriptions → Markdown, dropped here as:
- `er_diagrams/` — ER diagrams (text/Mermaid export)
- `user_guides/` — user guides (PDF/MD)
- `wireframes/` — wireframe exports with text annotations

One frame/diagram = one chunk. Metadata: `figma_file`, `frame_name`,
`source_type="design"`
