# Fetch a JIRA ID and create a Test Plan Generator and Tet Strategy Buddy
# KAN-4 -> Fetch Test Plan

Can you please read the B.L.A.S.T.md and objective.md again and create a lightweight React application which will take:
-the Jira Configuration
-Jira email ID
-Jira Token
-my Jira base URL
-the connection to my OpenGPT 120billion group API key, which is GROQ
-GROQ connection API details in the settings and take the JIRA ID and create the TestPaln automatically
you will able to craete a test plan based on the by fetching the KAN-4 automatically.

GROQ - openai/gpt-oss-120b (FREE)
Fetch JIRA -> email, token, JIRA ID KAN-4